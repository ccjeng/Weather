<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
        <ListPreference
            android:key="unit_list"
            android:defaultValue="celsius"
            android:entries="@array/pref_list_units"
            android:entryValues="@array/pref_list_units_values"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"
            android:title="@string/Units"
            android:summary="@string/pref_description_units" />
        <SwitchPreference
            android:key="background_photo"
            android:title="@string/preference_background_photo"
            android:summary="@string/preference_background_photo_summary"
            android:defaultValue="false" />

</PreferenceScreen>